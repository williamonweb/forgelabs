"use client"; import { FormEvent,useState } from "react";
export default function ContactForm(){const[sent,setSent]=useState(false);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const msg=`Olá, vim pelo site da Forge Labs.%0A%0AMeu nome: ${encodeURIComponent(String(f.get("nome")||""))}%0AEmpresa: ${encodeURIComponent(String(f.get("empresa")||""))}%0AProjeto: ${encodeURIComponent(String(f.get("tipo")||""))}%0A%0A${encodeURIComponent(String(f.get("mensagem")||""))}`;setSent(true);window.open(`https://wa.me/?text=${msg}`,"_blank","noopener,noreferrer")}return <form className="contact-form" onSubmit={submit}><div className="form-row"><label>Seu nome<input required name="nome" placeholder="Como podemos chamar você?"/></label><label>Empresa ou marca<input name="empresa" placeholder="Nome do seu negócio"/></label></div><label>O que você precisa?<select required name="tipo" defaultValue=""><option value="" disabled>Selecione uma opção</option><option>Site profissional</option><option>Sistema sob medida</option><option>Loja virtual</option><option>Automação ou integração</option><option>Manutenção e evolução</option><option>Outro projeto</option></select></label><label>Conte um pouco sobre a ideia<textarea required name="mensagem" rows={6} placeholder="Qual problema você quer resolver?"/></label><button type="submit">Continuar pelo WhatsApp <span>↗</span></button>{sent&&<p className="form-success">Pronto! Abrimos sua mensagem no WhatsApp.</p>}</form>}
